---
title: LLVM Tabanlı, AOT-Derlenen Programlama Dili
description: Python kadar kolay, C kadar hızlı. LLVM 18 arka uçlu, statik tipli, AOT-derlenen bir dil — HTTP / JSON / SQLite / OpenAPI yığını runtime'a gömülü, framework gerekmez.
template: splash
---

import TulparPlayground from '../../../components/TulparPlayground.astro';

<div class="tlp-landing">

<section class="tlp-hero">
	<div class="tlp-hero__content">
		<span class="tlp-eyebrow">AOT · LLVM 18 · piller dahil</span>
		<h1 class="tlp-hero__title">Üretim API'lerini<span>framework olmadan kur</span></h1>
		<p class="tlp-hero__lead">
			Python kadar kolay, C kadar hızlı. Statik tipli, AOT-derlenen bir dil —
			HTTP, JSON, SQLite, bir ORM ve OpenAPI doğrudan runtime'a gömülü.
			<code>npm install</code> yok, pip yok, bağımlılık yok.
		</p>
		<div class="tlp-hero__actions">
			<a class="tlp-btn tlp-btn--primary" href="/tr/intro/getting-started/">Başlarken →</a>
			<a class="tlp-btn tlp-btn--ghost" href="#playground">Playground'u Aç</a>
			<a class="tlp-btn tlp-btn--minimal" href="https://github.com/hamer1818/TulparLang">GitHub'da Gör ↗</a>
		</div>
		<div class="tlp-hero__stats">
			<div class="tlp-hero__stat"><b>~36k</b><span>istek/sn · Tulpar pool</span></div>
			<div class="tlp-hero__stat"><b>~1.9× C</b><span>fib · yerel AOT</span></div>
			<div class="tlp-hero__stat"><b>0</b><span>dış bağımlılık</span></div>
		</div>
	</div>

	<div class="tlp-hero__preview">
		<div class="tlp-window">
			<div class="tlp-window__bar">
				<span class="tlp-window__dot tlp-window__dot--r"></span>
				<span class="tlp-window__dot tlp-window__dot--y"></span>
				<span class="tlp-window__dot tlp-window__dot--g"></span>
				<span class="tlp-window__name">api.tpr</span>
				<span class="tlp-window__live">çalışıyor</span>
			</div>

			```tulpar
			import "wings";
			import "orm";

			orm_open("app.db");
			define_model("users", { "name": "TEXT", "age": "INTEGER" });

			func list_users(req)  { return ok(orm_all("users")); }
			func create_user(req) { return created(orm_create("users", req.json)); }

			get("/users",  "list_users");
			post("/users", "create_user");

			serve(8080);   // + Swagger UI, /openapi.json, /metrics, /healthz
			```
		</div>
	</div>
</section>

<section class="tlp-section">
	<div class="tlp-section__head">
		<span class="tlp-eyebrow">Benchmark</span>
		<h2>Önemli yerde hızlı</h2>
		<p><code>GET /</code> → JSON <code>{"{"}"hello":"world"{"}"}</code> throughput'u, keep-alive, native yük aracı, tek 14-vCPU kutu. Her runtime kendi önerilen tek-process yapılandırmasında.</p>
	</div>

	<div class="tlp-bench">
		<div class="tlp-bench__row tlp-bench__row--hero">
			<div class="tlp-bench__name">Tulpar<span class="tlp-bench__tag">pool · 14c</span></div>
			<div class="tlp-bench__track"><div class="tlp-bench__fill" style="--val:100%"></div></div>
			<div class="tlp-bench__val">~36k istek/sn</div>
		</div>
		<div class="tlp-bench__row">
			<div class="tlp-bench__name">Go net/http<span class="tlp-bench__tag">14c</span></div>
			<div class="tlp-bench__track"><div class="tlp-bench__fill" style="--val:83%"></div></div>
			<div class="tlp-bench__val">~30k istek/sn</div>
		</div>
		<div class="tlp-bench__row">
			<div class="tlp-bench__name">Node.js http<span class="tlp-bench__tag">1c</span></div>
			<div class="tlp-bench__track"><div class="tlp-bench__fill" style="--val:24%"></div></div>
			<div class="tlp-bench__val">~8.7k istek/sn</div>
		</div>
		<div class="tlp-bench__row">
			<div class="tlp-bench__name">FastAPI<span class="tlp-bench__tag">uvicorn · 1w</span></div>
			<div class="tlp-bench__track"><div class="tlp-bench__fill" style="--val:10%"></div></div>
			<div class="tlp-bench__val">~3.5k istek/sn</div>
		</div>
		<p class="tlp-bench__note">
			Üstelik latency düşük kalıyor: <strong>p50 0.32 ms</strong> (Tulpar) vs 3.3 ms (FastAPI). CPU'da fib, <strong>1.37× C</strong> (Windows / MinGW) ile <strong>~1.9× C</strong> (Linux gcc -O2) arasında — her iki ortamda da Rust/Go sınıfı. <a href="/tr/ecosystem/benchmarks/">Tüm metodoloji →</a>
		</p>
	</div>
</section>

<section class="tlp-section">
	<div class="tlp-section__head">
		<span class="tlp-eyebrow">Özellikler</span>
		<h2>İhtiyacın olan her şey zaten kutuda</h2>
	</div>

	<div class="tlp-bento">
		<div class="tlp-bento__item tlp-bento__item--feature">
			<div class="tlp-bento__icon">🚀</div>
			<h3>İlk günden API</h3>
			<p><code>import "wings"</code>, bir route kaydet, <code>serve(8080)</code> — bitti. Otomatik OpenAPI 3.0 + Swagger UI, şema doğrulama (geçersiz gövde → 422), bağımlılık enjeksiyonu, yapılandırılmış log, keep-alive, ayrıca bedava gelen yerleşik <code>/healthz</code> ve <code>/metrics</code> (JSON + Prometheus).</p>
		</div>
		<div class="tlp-bento__item">
			<div class="tlp-bento__icon">⚡</div>
			<h3>C sınıfı performans</h3>
			<p>LLVM 18 ile bağımsız yerel binary'ye AOT-derlenir.</p>
		</div>
		<div class="tlp-bento__item">
			<div class="tlp-bento__icon">🗄️</div>
			<h3>SQLite + ORM</h3>
			<p>Gömülü SQLite ve Active-Record ORM — bağlanacak sürücü yok.</p>
		</div>
		<div class="tlp-bento__item">
			<div class="tlp-bento__icon">🧰</div>
			<h3>Editör araçları</h3>
			<p>Gömülü LSP, <code>tulpar fmt</code> ve bir VS Code eklentisi.</p>
		</div>
		<div class="tlp-bento__item">
			<div class="tlp-bento__icon">📦</div>
			<h3>Paket yöneticisi</h3>
			<p><code>tulpar pkg</code>, <code>tulpar.toml</code> manifesti ve kilit dosyası.</p>
		</div>
		<div class="tlp-bento__item">
			<div class="tlp-bento__icon">🌍</div>
			<h3>UTF-8 · TR + EN</h3>
			<p>Unicode tanımlayıcılar ve iki dilli derleyici tanıları.</p>
		</div>
	</div>
</section>

<section id="playground" class="tlp-section">
	<div class="tlp-section__head">
		<span class="tlp-eyebrow">Canlı playground</span>
		<h2>Doğrudan tarayıcında dene</h2>
		<p>Kurulum yok, ayar yok. Kodu düzenle ve <strong>Çalıştır</strong>'a bas — sayfa içinde çalışır.</p>
	</div>

	<TulparPlayground
		title="merhaba.tpr — özyineleme + ternary operatörü"
		rows={9}
		code={`// Klasik özyineleme, tek satır dallar:  cond ? a : b
func fib(int n) {
    return n < 2 ? n : fib(n - 1) + fib(n - 2);
}

for (int i = 0; i < 12; i++) {
    print("fib(" + toString(i) + ") = " + toString(fib(i)));
}`}
	/>
</section>

<section class="tlp-section">
	<div class="tlp-section__head">
		<span class="tlp-eyebrow">Sıfırdan bir REST API'ye</span>
		<h2>Tek dosyada kalıcı bir CRUD servisi</h2>
		<p>Model, route'lar ve sunucu tek bir <code>.tpr</code> dosyasında — <code>/healthz</code>, <code>/metrics</code>, <code>/openapi.json</code> ve bir Swagger UI ile bedava açılır.</p>
	</div>

```tulpar
import "wings";
import "orm";

orm_open("app.db");
define_model("users", {
    "id":   "INTEGER PRIMARY KEY AUTOINCREMENT",
    "name": "TEXT NOT NULL",
    "age":  "INTEGER"
});

func list_users(req)  { return ok(orm_all("users")); }

func create_user(req) {
    int id = orm_create("users", req.json);
    return created(orm_find("users", id));
}

get("/users",  "list_users");
post("/users", "create_user");
body_schema({"name": "str", "age?": "int"});   // geçersiz gövde → otomatik 422

serve(8080);
```

Rehberli halini mi istersin? **[Wings Öğreticisi](/tr/ecosystem/wings-tutorial/)**
üç tam uygulamayı adım adım kurar — REST, token auth ve SQLite destekli bir API.

</section>

<div class="tlp-cta">
	## Tek komutla hazır

	<p>Araç zincirini kur, ilk programını derleyip çalıştır. macOS, Linux ve Windows.</p>

	<div class="tlp-cta__cmd"><span class="tlp-prompt">$ </span>curl -fsSL https://tulparlang.dev/install.sh | bash</div>

	<div style="margin-top:1.5rem">
		[Başlarken →](/tr/intro/getting-started/) &nbsp;·&nbsp; [Kurulum](/tr/intro/installation/) &nbsp;·&nbsp; [Dil Rehberi](/tr/guide/syntax/)
	</div>
</div>

</div>
