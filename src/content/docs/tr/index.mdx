---
title: Tulpar Dili
description: Python kadar kolay, C kadar hızlı. LLVM 18 arka ucu, kutudan çıkan HTTP/JSON/SQLite yığını ve tam editör araçlarıyla statik tipli bir dil.
template: splash
hero:
  tagline: "<strong>Python kadar kolay, C kadar hızlı.</strong> LLVM 18 arka uçlu, statik tipli ve AOT-derlenen bir dil — kutudan çıkan HTTP / JSON / SQLite yığını ve tam editör araçlarıyla."
  image:
    file: ../../../assets/icon.png
  actions:
    - text: Başlarken
      link: /tr/intro/getting-started/
      icon: right-arrow
      variant: primary
    - text: Hemen dene ↓
      link: "#hemen-dene"
      icon: rocket
      variant: secondary
    - text: GitHub'da Görüntüle
      link: https://github.com/hamer1818/TulparLang
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import TulparPlayground from '../../../components/TulparPlayground.astro';

<div class="tlp-stats">
	<div class="tlp-stat">
		<div class="tlp-stat__num">1.3–1.4×</div>
		<div class="tlp-stat__label">sıkı döngülerde C hızı</div>
	</div>
	<div class="tlp-stat">
		<div class="tlp-stat__num">~24k</div>
		<div class="tlp-stat__label">HTTP istek/sn, tek thread</div>
	</div>
	<div class="tlp-stat">
		<div class="tlp-stat__num">0</div>
		<div class="tlp-stat__label">npm / pip kurulumu</div>
	</div>
	<div class="tlp-stat">
		<div class="tlp-stat__num">LLVM 18</div>
		<div class="tlp-stat__label">yerel AOT arka uç</div>
	</div>
	<div class="tlp-stat">
		<div class="tlp-stat__num">TR + EN</div>
		<div class="tlp-stat__label">iki dilli derleyici</div>
	</div>
</div>

<div id="hemen-dene" class="tlp-try">
	<span class="tlp-eyebrow">Şimdi çalıştır</span>

	Kurulum yok, ayar yok — bu kod **doğrudan tarayıcında** çalışıyor. Kodu düzenle
	ve **Çalıştır**'a bas.

	<TulparPlayground
		title="merhaba.tpr — özyineleme + ternary operatörü"
		rows={9}
		code={`// Klasik özyineleme, tek satır dallar:  cond ? a : b
func fib(int n) {
    return n < 2 ? n : fib(n - 1) + fib(n - 2);
}

for (int i = 0; i < 12; i++) {
    print("fib(" + toString(i) + ") = " + toString(fib(i)));
}`}
	/>
</div>

## Neden Tulpar

<CardGrid stagger>
	<Card title="C sınıfı performans" icon="rocket">
		LLVM ile AOT-derlenir. Aynı donanımda sıkı döngülerde **C'nin 1.3–1.4× ı** aralığında — Rust ve Go ile aynı mahallede. HTTP sunucusu tek thread'de ~24k istek/sn, Node.js seviyesinde.
	</Card>
	<Card title="İlk günden API" icon="seti:json">
		`import "wings"`, bir route kaydet, `serve(8080)` — bitti. Yerleşik `/healthz`, `/metrics` (JSON + Prometheus), otomatik OpenAPI 3.0 + Swagger UI, şema doğrulama, bağımlılık enjeksiyonu, yapılandırılmış log, keep-alive, çok-thread'li istek işleme.
	</Card>
	<Card title="Modern editör araçları" icon="laptop">
		Gömülü dil sunucusu: tanılama, hover, tamamlama, tanıma-git, referans-bul, yeniden adlandır. Ayrıca `tulpar fmt` (gofmt tarzı idempotent biçimlendirici) ve sorunsuz çalışan bir VS Code eklentisi.
	</Card>
	<Card title="Paket yöneticisi" icon="puzzle">
		`tulpar pkg init / add / install / list / remove`, `tulpar.toml` manifesti, `tulpar.lock` kilit dosyası. Yerel yolları vendor'la, tek-dosya URL'leri çek veya registry'den semver ile al.
	</Card>
	<Card title="Piller dahil" icon="seti:db">
		Gömülü SQLite + Active-Record ORM. Dışa HTTP/HTTPS istemcisi. Regex, CSV, dosya glob, datetime (ISO 8601 ayrıştır/biçimlendir/gün), JSON, thread, mutex, soket — hepsi runtime'da, `npm install` yok.
	</Card>
	<Card title="UTF-8 · Türkçe · English" icon="pencil">
		Tanımlayıcılar, metinler, yollar — hepsi UTF-8. Derleyici hata mesajları iki dilli (Türkçe + İngilizce, `tr_en` yerelleştirmesiyle). Çapraz platform: Linux, macOS, Windows.
	</Card>
</CardGrid>

## Sıfırdan bir REST API'ye

Gerçek, kalıcı bir CRUD API — model, route'lar, sunucu — tek dosyada. Sunucu
`/healthz`, `/metrics`, `/openapi.json` ve bir Swagger UI ile bedava açılır.

```tulpar
import "wings";
import "orm";

orm_open("app.db");
define_model("users", {
    "id":   "INTEGER PRIMARY KEY AUTOINCREMENT",
    "name": "TEXT NOT NULL",
    "age":  "INTEGER"
});

func list_users(req)  { return ok(orm_all("users")); }

func create_user(req) {
    int id = orm_create("users", req.json);
    return created(orm_find("users", id));
}

get("/users",  "list_users");
post("/users", "create_user");
body_schema({"name": "str", "age?": "int"});   // geçersiz gövde → otomatik 422

serve(8080);
```

Rehberli halini mi istersin? **[Wings Öğreticisi](/tr/ecosystem/wings-tutorial/)**
üç tam uygulamayı adım adım kurar — REST, token auth ve SQLite destekli bir API.

<div class="tlp-cta">
	## Tek komutla hazır

	<p>Araç zincirini kur, ilk programını derleyip çalıştır. macOS, Linux ve Windows.</p>

	<div class="tlp-cta__cmd"><span class="tlp-prompt">$ </span>curl -fsSL https://tulparlang.dev/install.sh | bash</div>

	<div style="margin-top:1.5rem">
		[Başlarken →](/tr/intro/getting-started/) &nbsp;·&nbsp; [Kurulum](/tr/intro/installation/) &nbsp;·&nbsp; [Dil Rehberi](/tr/guide/syntax/)
	</div>
</div>
