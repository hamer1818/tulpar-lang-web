---
title: Benchmark Sonuçları
description: Tulpar'ın CPU ve HTTP iş yüklerinde C / Rust / Go / Node.js / Python karşısında nasıl performans verdiği.
---

Tulpar'ın sloganı "Python kadar kolay, C kadar hızlı". CPU tarafının
iddiasını her commit'te doğruluyoruz; HTTP tarafı bizi tek-thread Node.js
seviyesine çıkarıyor.

## CPU benchmark'ları

`benchmarks/loopsum.tpr` ve `benchmarks/fib.tpr`, native LLVM `i64`
codegen için typed AOT path (explicit `: int` return) kullanır.

| Benchmark               | C (gcc -O2) | Tulpar AOT | Oran        |
| ----------------------- | ----------: | ---------: | ----------- |
| `loopsum` (10M sum)     | 67 ms       | 88 ms      | **1.31× C** |
| `fib(35)` recursive     | 83 ms       | 114 ms     | **1.37× C** |

Tulpar AOT **1.3–1.4× C** aralığında — Rust ve Go ile aynı bölgede.
Best-of-3 wall-clock, Windows 11 + MinGW64.

## HTTP throughput

`benchmarks/http_bench.py` — 5000 GET, 4 keep-alive bağlantı,
single-thread sunucular, JSON `{"hello":"world"}` body.

| Sunucu                  | Wall (s) | req/sec | Notlar                                                              |
| ----------------------- | -------: | ------: | ------------------------------------------------------------------- |
| Tulpar Wings            | 0.193    | ~26 000 | Single-thread `listen()`, NODELAY, dlsym handler cache.             |
| Node.js http            | 0.184    | ~27 200 | `http.createServer`, V8 22.x.                                       |
| Python ThreadingHTTP    | 0.354    | ~14 100 | `ThreadingHTTPServer`, tek CPython process.                         |

`listen_async()` multi-thread variant'ı sunar — handler dispatch hâlâ
`_wings_handler_mu` altında serileşiyor (LLVM thread-local global'leri
inene kadar), ama paralel `recv` / `send` keep-alive iş yükünde
throughput yükselmesi sağlıyor.

## Bizi buraya getirenler

**Uygulanan hot-path optimizasyonları:**

- `call(handler_name)` dlsym cache (256-slot FNV-1a hash) — request başı sembol-tablosu walk'unu eliyor.
- Accept'te TCP_NODELAY — Nagle'ın 40ms gecikmesini kaldırır (+%13).
- Static thread-local recv buffer — keep-alive request başı 64KB malloc/free pair'ini düşürür.
- Per-request arena reset — uzun süreli sunucularda bounded memory.
- `break` / `continue` real codegen — eskiden silently no-op'tu; LLVM induction-variable update'lerinde phi node optimize edemiyordu.
