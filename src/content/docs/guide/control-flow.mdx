---
title: Control Flow
description: Learn about control flow statements in Tulpar.
---

import TulparPlayground from '../../../components/TulparPlayground.astro';

## If-Else Statements

<TulparPlayground
	title="If / Else"
	code={String.raw`int age = 18;

if (age >= 18) {
    print("Adult");
} else {
    print("Minor");
}

// Logical operators
if (age >= 18 && age < 65) {
    print("Working age");
}`}
/>

## Ternary Operator

`cond ? then : else` is the expression form of if/else — it **produces a value**,
so you can use it inline in an assignment, a function argument, or a `return`.
Only the chosen branch is evaluated (lazy), so a side effect in the untaken
branch never fires.

<TulparPlayground
	title="Ternary"
	code={String.raw`int age = 20;
str status = age >= 18 ? "adult" : "minor";
print(status);                       // adult

// Nesting is right-associative — a clean grade ladder
func grade(int s) {
    return s >= 90 ? "A" : s >= 80 ? "B" : s >= 70 ? "C" : "F";
}
print(grade(85));                    // B

// Works inline as a function argument
func abs(int n) { return n < 0 ? 0 - n : n; }
print(abs(0 - 42));                  // 42`}
/>

It binds looser than every other operator (including `&&` / `||`), so
`a + b > 4 ? x : y` parses as `((a + b) > 4) ? x : y`, and
`a ? b : c ? d : e` parses as `a ? b : (c ? d : e)`.

## While Loop

<TulparPlayground
	title="While Loop"
	code={String.raw`int i = 0;
while (i < 10) {
    print(i);
    i++;
}`}
/>

## For Loop

Tulpar supports both C-style for loops and for-each loops:

<TulparPlayground
	title="For Loops"
	code={String.raw`// C-style for loop
for (int i = 0; i < 10; i++) {
    print("i =", i);
}

// For-each with range
for (i in range(10)) {
    print("i =", i);
}`}
/>

## Match Expressions

`match` is a Rust-style pattern match. Unlike a C `switch`, it is an
**expression** — it produces a value, so you can assign it directly. Each arm
is `pattern => body`; `_` is the catch-all wildcard. Arms support single
literals, `|`-alternatives, and inclusive `lo .. hi` ranges.

<TulparPlayground
	title="Match as an expression"
	code={String.raw`func grade(int score) {
    return match score {
        90 => "A",
        80 => "B",
        70 => "C",
        _  => "F"
    };
}

print(grade(90));   // A
print(grade(55));   // F  (no literal matched, fell through to _)`}
/>

Patterns can match a set of values or a range. The subject is evaluated once,
and the first matching arm wins:

<TulparPlayground
	title="Alternatives and ranges"
	code={String.raw`func bucket(int n) {
    return match n {
        0          => "zero",
        1 | 2 | 3  => "small",
        10 .. 20   => "teens",
        _          => "big"
    };
}

print(bucket(2));    // small
print(bucket(15));   // teens
print(bucket(99));   // big`}
/>

`match` also works in statement position with block arms, and the subject may
be any expression (int, string, or bool):

<TulparPlayground
	title="Statement-position match"
	code={String.raw`str cmd = "stop";
match cmd {
    "go"   => { print("moving"); },
    "stop" => { print("halted"); },
    _      => { print("idle"); }
}`}
/>

:::note
v1 patterns match scalar values (int / string / bool). Struct/array
destructuring patterns are planned for a later release.
:::
